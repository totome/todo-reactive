services:
  mongodb:
    image: 'mongo'
    volumes:
      - data:/data/db
      - ./env/init-mongo.js:/docker-entrypoint-initdb.d/mongo-init.js:ro
    env_file:
      - ./env/mongo.properties
    ports:
      - '27017:27017'
volumes:
  data: